echo "🧪 Running tests..."
yarn test

if [ $? -ne 0 ]; then
  echo "❌ Tests failed! Please fix them before committing."
  exit 1
fi

echo "✅ Tests passed!"

echo "🎨 Running linter, typecheck, and formatter..."
yarn lint:fix && yarn typecheck && yarn format

if [ $? -ne 0 ]; then
  echo "❌ Fail in lint or typecheck! Please fix them before committing."
  exit 1
fi

echo "✅ All checks passed!"
